# .env

# ═══════════════════════════════════════════════════════════════
# DATABASE (Docker Local)
# ═══════════════════════════════════════════════════════════════

POSTGRES_USER=healthcheck
POSTGRES_PASSWORD=healthcheck123
POSTGRES_DB=healthcheck
POSTGRES_PORT=5432

# Connection string (Docker local)
DATABASE_URL=postgresql://healthcheck:healthcheck123@localhost:5432/healthcheck

# ═══════════════════════════════════════════════════════════════
# JWT
# ═══════════════════════════════════════════════════════════════

JWT_SECRET=
JWT_EXPIRES_IN=
JWT_REFRESH_EXPIRES_IN=